# 智教通验收检查记录

检查时间：2026-07-07 20:10

## 结论

当前代码和本地环境已达到 MVP 冒烟验收标准：服务可用、种子账号可登录、5 个正式角色的菜单权限和关键页面可打开、前端可构建。

任务清单 `outputs/DEV-TASKS.md` 的 `111/111` 可以作为开发完成清单，但不能等价为完整验收证明。完整 V1.0 规格里的高并发选课、1000 QPS 性能、抽签选课等指标尚未做压测或专项测试。

## 本次验证

| 项目 | 结果 | 说明 |
|---|---:|---|
| 后端健康检查 | 通过 | `/actuator/health` 返回 `UP` |
| API 冒烟测试 | 通过 | `outputs/smoke-test.py`，12/12 通过 |
| 前端构建 | 通过 | `npm run build` 成功 |
| 角色菜单绑定 | 通过 | STUDENT 7、TEACHER 5、ACADEMIC 13、ADMIN 5、QB_ADMIN 3 |
| 接口权限隔离 | 通过 | 5 个角色允许接口 200，禁止接口 403 |
| 页面烟测 | 通过 | 24 个核心页面无 500/404、无横向溢出 |

## 发现并处理的问题

1. 教师角色缺少“我的课表”菜单绑定。
   - 已补齐本地数据库 `sys_role_menu (role_id=2, menu_id=3)`。
   - 已更新 `outputs/role-menu-bindings-v1.sql`。
   - 已调整前端 `/schedule` 路由允许 `student,teacher`。

2. 冒烟脚本健康检查曾把 Vite 返回的 HTML 当成通过。
   - 已改为必须解析 JSON 且 `status=UP` 才算通过。

## 清单与实际功能差异

1. 教师端“教学评价”不属于教师菜单，实际菜单表中归属教务端，这是合理口径。
2. 管理员没有独立“菜单管理”页面，菜单授权能力在“角色管理”页面中完成。
3. 菜单表里不是“5 个工作台”，而是 5 个角色各自有一个工作台入口；这是合理的，因为不同角色工作台路径和展示内容不同。

## 未覆盖项

1. 未执行 200 并发选课不超卖专项测试。
2. 未执行 1000 QPS、P99 <= 2s 性能验收。
3. 未验证抽签选课闭环；当前更接近实时抢课/容量校验模式。
4. 未做完整人工 CRUD 回归，页面烟测只验证页面可打开和基础布局稳定。
